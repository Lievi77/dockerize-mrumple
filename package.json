{
  "scripts": {
    "install": "(cd backend && npm install) && (cd frontend && npm install)",
    "start": "(cd backend && node app) & (cd frontend && npm start)",
    "dev": "(cd backend && npm run dev) & (cd frontend && npm start)"
  },
  "dependencies": {
    "create-react-app": "^5.0.0"
  }
}